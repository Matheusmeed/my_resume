import styled from 'styled-components';
import { SectionScreenWrapper } from '../../shared/styles/styles';

export const AboutMeSectionDiv = styled(SectionScreenWrapper)`
  background: linear-gradient(to right, #0d1e30, #194058);
`;
