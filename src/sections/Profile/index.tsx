import { SectionScreenWrapper } from '../../shared/styles/styles';

const Profile = () => {
  return (
    <SectionScreenWrapper backgroundColor='white'>Profile</SectionScreenWrapper>
  );
};

export default Profile;
